template
  div
    h1 ${this.inputvls}
    ol
      li(each="todo in this.todos")
        em ${todo}
    input( 
      type="text", 
      value="${new String('')}", 
      change.bind="this.desc")